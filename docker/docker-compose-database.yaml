include:
  - ./docker-compose-networks.yaml

services:
  database:
    image: postgres:17.0-alpine3.19@sha256:79ac4cc9379deb347f0262b3c97f92e4aff761905b1aef0ab423ebd29ac7d596
    environment:
      - POSTGRES_USER=mldemo
      - POSTGRES_PASSWORD=mldemo
    # volumes:
    #   - /var/mldemo/volumes/lib/postgresql/data/:/var/lib/postgresql/data/
    ports:
      - "5432:5432"
    networks:
      - backend
    restart: always
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U $${POSTGRES_USER}']
      interval: 5s
      timeout: 5s
      retries: 10
